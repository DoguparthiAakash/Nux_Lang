class Test {
    var val;
    func set_val(v) {
        # Check if we can access val? 
        # Or if we have 'this'?
        # For now, let's just print.
        print("Set Val Calls: ");
        println(v);
    }
}

func main() {
    var t = new Test();
    t.set_val(42);
}
main();
